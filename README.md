Would replace the queue, with a List. 
Right now it is not possible to specify what task you would want to complete, it has to be in the order of the queue (ofc).
Would benefit to replace the queue with a list, so you could specify what task you would want to complete.
